package panels
{
	import org.aswing.JPanel;
	import org.aswing.LayoutManager;
	
	public class HeadPane extends JPanel
	{
		public function HeadPane(layout:LayoutManager=null)
		{
			super(layout);
			name = "head";
		}
	}
}